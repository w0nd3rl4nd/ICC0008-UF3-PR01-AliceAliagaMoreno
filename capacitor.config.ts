import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ICC0008-UF3-PR01-AliceAliagaMoreno',
  webDir: 'www'
};

export default config;
